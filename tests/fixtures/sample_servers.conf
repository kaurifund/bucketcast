#===============================================================================
# TEST FIXTURE - Sample Servers Configuration
#===============================================================================
# This is a test fixture used by the test suite.
# DO NOT use in production.
#===============================================================================

declare -A server_fixture_dev=(
    [name]="Fixture Dev Server"
    [host]="dev.fixture.local"
    [port]="22"
    [user]="testdev"
    [remote_base]="/home/testdev/.sync-shuttle"
    [enabled]="true"
    [s3_backup]="false"
)

declare -A server_fixture_prod=(
    [name]="Fixture Prod Server"
    [host]="prod.fixture.local"
    [port]="2222"
    [user]="testprod"
    [remote_base]="/var/sync-shuttle"
    [enabled]="true"
    [s3_backup]="true"
)

declare -A server_fixture_disabled=(
    [name]="Fixture Disabled Server"
    [host]="disabled.fixture.local"
    [port]="22"
    [user]="nobody"
    [remote_base]="/nonexistent"
    [enabled]="false"
    [s3_backup]="false"
)
